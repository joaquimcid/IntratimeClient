<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="IntratimeClient.View.MenuPage">
    <ContentPage.ToolbarItems>
    </ContentPage.ToolbarItems>
    <ContentPage.Content>
        <StackLayout VerticalOptions="CenterAndExpand">
            
            <Button Text="Create Clock Records" Command="{Binding BulkClockCommand}" />
            <Button Text="List Clocks Records" Command="{Binding ListClockCommand}" />

            <Label x:Name="MessageLabel" Text="{Binding MessageLabel}" />
        </StackLayout>
    </ContentPage.Content>
</ContentPage>